{
	"description": "Generate a bash script with given specifications.",
	"category": "code-generation",
	"tags": [ "script" ],
	"language": "Bash",
	"template": "Write a bash script named `{file_name}` that {purpose}.\n\rRequirements:\n{requirements}}\n\rOutput the code in a markdown code block with language specifier:filename e.g. ````Bash:FileName\n\rCodeContent\n\r````.",
	"parameters": [ "file_name", "purpose", "requirements" ],
	"defaults": {
		"requirements": "Using modern bash(features and best practices).",
		"file_name": "save-screenshot.sh",
		"purpose": "Create a bash script takes a single screenshot and saves it into a target folder (default is the user's screenshots folder)"
	},
	"version": "1.0.0"
}
